#include "main.h"
#include <stdio.h>

/**
 * code that prints _putchar
 * main: Entry point
 * return: always 0
 */

int main()
{
	*sh = "_putchar";

	while (*sh)
	{
		_putchar(*sh);

		word++;
	}

	return (0);
}
