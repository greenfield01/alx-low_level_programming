#include "main.h"
#include <stdio.h>

/**
 * code that prints _putchar
 * main: Entry point
 * return: always 0
 */

int main()
{

	*letters = "_putchar";

	while (*letters)
	{
		_putchar(*letters);

		letters++;
	}

	_putchar(\n);

	return (0);
}
